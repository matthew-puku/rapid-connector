<%= form_with(model: rapid_connection) do |form| %>
  <% if rapid_connection.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(rapid_connection.errors.count, "error") %> prohibited this rapid_connection from being saved:</h2>

      <ul>
        <% rapid_connection.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nickname, style: "display: block" %>
    <%= form.text_field :nickname %>
  </div>

  <div>
    <%= form.label :secret, style: "display: block" %>
    <%= form.text_field :secret %>
  </div>

  <div>
    <%= form.label :callback_url, style: "display: block" %>
    <%= form.text_field :callback_url %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
